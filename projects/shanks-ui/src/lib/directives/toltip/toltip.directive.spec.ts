import { ToltipDirective } from './toltip.directive';

describe('ToltipDirective', () => {
  it('should create an instance', () => {
    const directive = new ToltipDirective();
    expect(directive).toBeTruthy();
  });
});
